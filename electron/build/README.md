# electron/build/

`icon.ico` موجود بالفعل هنا — أيقونة بسيطة بهوية العلامة (خلفية داكنة +
نبضة خضراء) بـ ٧ دقات وضوح (16 إلى 256px)، صالحة تقنياً لتشغيل
`npm run dist:win` دون أخطاء. استبدلها بأيقونة احترافية نهائية قبل الإصدار
التجاري الفعلي — هذه كافية فقط لإخراج بناء وظيفي.
